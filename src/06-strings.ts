(() => {
    let productTitle = 'Product 1';
    let productName = 'String'
    let productPrice = 200;
    let isNew = false;

    const summary = `
    The product: ${productTitle}
    descripion: ${productName}
    price: ${productPrice}
    isNew: ${isNew}
    `;
console.log(summary);
})();